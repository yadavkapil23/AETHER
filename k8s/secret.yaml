# Placeholder values — do not commit real secrets to git.
# Override with `kubectl create secret generic aether-secrets --from-literal=...`
# or a sealed-secrets / external-secrets tool in real deployments.
apiVersion: v1
kind: Secret
metadata:
  name: aether-secrets
type: Opaque
stringData:
  DATABASE_URL: "postgresql://postgres:password@postgres:5432/aether_gateway"
  JWT_SECRET: "change-me-in-production"
  API_KEYS: "sk-demo123"
  HUGGINGFACE_API_KEY: ""
  POSTGRES_PASSWORD: "password"
